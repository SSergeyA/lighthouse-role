server {
    listen       80;
    server_name  localhost;

    access_log /var/log/nginx/{{ lighthouse_access_log_name }}_access_log;
    error_log /var/log/nginx/{{ lighthouse_access_log_name }}_error_log;

    location / {
        root   {{ lighthouse_location_dir }};
        index   index.html;
    }
}